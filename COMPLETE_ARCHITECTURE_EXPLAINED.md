# Complete SRE Agent Architecture - Detailed Explanation

## ✅ Verified Against Official AWS Diagram

The official architecture diagram shows these key components:

**Inside AWS Cloud:**
1. **AgentCore Runtime** - Container running your SRE Support Agent (multi-agent collaboration)
2. **AgentCore Gateway** - Translates OpenAPI specs to MCP tools, handles authentication
3. **AgentCore Memory** - Stores session and previous interactions
4. **AgentCore Identity (Amazon Cognito)** - Authenticates users
5. **Amazon Bedrock LLMs** - Powers the AI (Claude/Nova models)
6. **AgentCore Observability** - Monitors agent traces

**Outside AWS Cloud (Your Infrastructure):**
- **4 Backend API Tools** (shown with 🗄️ database icons):
  - Tool 1: k8s-api__get_cluster_events()
  - Tool 2: logs-api__analyze_log_patterns()
  - Tool 3: metrics-api__analyze_trends()
  - Tool 4: runbooks-api__search_runbooks()

**The Flow:**
```
Customer → AgentCore Runtime → AgentCore Gateway → Your Backend APIs
              ↓                       ↓
        Amazon Bedrock          AgentCore Identity
        AgentCore Memory
```

---

## Understanding the Architecture Diagram

Based on the architecture diagram in `docs/images/sre-agent-architecture.png`, let me explain what you're seeing:

### What You See in the Diagram

The diagram shows **4 backend API tools** (K8s API, Logs API, Metrics API, Runbooks API) as separate logical components. The diagram shows the **architecture** (what components exist), not the **deployment** (how many servers you need). You can deploy them on one EC2 instance or multiple - both are valid!

```
┌─────────────────────────────────────────────────────────────────┐
│ WHAT THE DIAGRAM SHOWS (Logical Architecture)                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐            │
│ │ K8s API │  │Logs API │  │Metrics  │  │Runbooks │            │
│ │         │  │         │  │  API    │  │  API    │            │
│ └─────────┘  └─────────┘  └─────────┘  └─────────┘            │
│                                                                 │
│ These are 4 SEPARATE PROGRAMS (processes)                      │
│ But they can all run on ONE computer!                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ HOW IT'S ACTUALLY DEPLOYED (Physical Architecture)             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌───────────────────────────────────────────────────────────┐  │
│ │ ONE EC2 INSTANCE (One Computer)                           │  │
│ │                                                           │  │
│ │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐     │  │
│ │  │ K8s API │  │Logs API │  │Metrics  │  │Runbooks │     │  │
│ │  │Port 8011│  │Port 8012│  │  API    │  │  API    │     │  │
│ │  │         │  │         │  │Port 8013│  │Port 8014│     │  │
│ │  └─────────┘  └─────────┘  └─────────┘  └─────────┘     │  │
│ │                                                           │  │
│ │  All 4 programs running on the SAME computer             │  │
│ └───────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Think of it like this:**
- Your laptop can run Chrome, Spotify, Word, and Excel at the same time, right?
- Same thing! One EC2 instance runs 4 backend programs at the same time
- Each program listens on a different "port" (like different doors on the same building)

---

## The Complete Architecture - Layer by Layer

Let me explain the ENTIRE system from top to bottom:

### Layer 1: The User (You!)

```
┌─────────────────────────────────────────────────────────────────┐
│ YOU (The User)                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ You ask a question:                                             │
│ "What pods are in CrashLoopBackOff?"                            │
│                                                                 │
│ You can ask via:                                                │
│ ├── Command Line: uv run sre-agent --prompt "..."              │
│ ├── Web Interface: (if you build one)                          │
│ ├── Slack Bot: (if you integrate)                              │
│ └── API Call: curl http://agent-runtime/invocations            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓
```

---

### Layer 2: The SRE Agent (Your AI Assistant)

```
┌─────────────────────────────────────────────────────────────────┐
│ SRE AGENT (Multi-Agent System)                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ SUPERVISOR AGENT (The Boss)                             │    │
│ │ ─────────────────────────────────────────────────────── │    │
│ │ • Receives your question                                │    │
│ │ • Breaks it down into tasks                             │    │
│ │ • Decides which specialist agents to use                │    │
│ │ • Coordinates the investigation                         │    │
│ │ • Remembers your preferences from past conversations    │    │
│ └─────────────────────────────────────────────────────────┘    │
│                            ↓                                    │
│         ┌──────────────────┼──────────────────┐                │
│         ↓                  ↓                  ↓                 │
│ ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│ │ Kubernetes   │  │ Logs Agent   │  │ Metrics      │  ...     │
│ │ Agent        │  │              │  │ Agent        │          │
│ │              │  │              │  │              │          │
│ │ Specialist   │  │ Specialist   │  │ Specialist   │          │
│ │ for K8s      │  │ for Logs     │  │ for Metrics  │          │
│ └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                 │
│ WHERE DOES THIS RUN?                                            │
│ ├── Option 1: Your laptop (for testing)                        │
│ ├── Option 2: Docker container on your laptop (for testing)    │
│ └── Option 3: AgentCore Runtime in AWS (for production)        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓
```

**Key Point:** The agent is YOUR code - the Python code you wrote/downloaded. It can run anywhere!

---

### Layer 3: AgentCore Gateway (AWS Service - The Middleman)

```
┌─────────────────────────────────────────────────────────────────┐
│ AGENTCORE GATEWAY (AWS Managed Service)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ This is like a "translator" and "security guard"                │
│                                                                 │
│ What it does:                                                   │
│ ├── Receives requests from your agent                          │
│ ├── Checks your JWT token (authentication)                     │
│ ├── Translates your request to the right backend API           │
│ ├── Gets the API key from Credential Provider                  │
│ ├── Calls the backend API with the API key                     │
│ └── Returns the response to your agent                         │
│                                                                 │
│ WHERE DOES THIS RUN?                                            │
│ └── AWS Cloud (Amazon manages this for you)                    │
│     You just configure it, you don't run it!                   │
│                                                                 │
│ YOUR GATEWAY:                                                   │
│ ├── ID: sre-gateway-rks2qobw3q                                 │
│ ├── URL: https://sre-gateway-rks2qobw3q.gateway...             │
│ └── Status: READY                                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓
```

**Key Point:** This is an AWS service. You don't install or run it. You just create it and configure it.

---

### Layer 4: Backend API Servers (Your Demo Servers)

```
┌─────────────────────────────────────────────────────────────────┐
│ BACKEND API SERVERS (Your Code)                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌───────────────────────────────────────────────────────────┐  │
│ │ ONE EC2 INSTANCE (or your laptop for testing)            │  │
│ │                                                           │  │
│ │  ┌─────────────────────────────────────────────────────┐ │  │
│ │  │ K8s API Server (Port 8011)                          │ │  │
│ │  │ ─────────────────────────────────────────────────── │ │  │
│ │  │ • Serves fake Kubernetes data                       │ │  │
│ │  │ • Returns pod lists, deployment status, etc.        │ │  │
│ │  │ • Reads from: backend/data/k8s_data/                │ │  │
│ │  └─────────────────────────────────────────────────────┘ │  │
│ │                                                           │  │
│ │  ┌─────────────────────────────────────────────────────┐ │  │
│ │  │ Logs API Server (Port 8012)                         │ │  │
│ │  │ ─────────────────────────────────────────────────── │ │  │
│ │  │ • Serves fake log data                              │ │  │
│ │  │ • Returns log entries, error logs, etc.             │ │  │
│ │  │ • Reads from: backend/data/logs_data/               │ │  │
│ │  └─────────────────────────────────────────────────────┘ │  │
│ │                                                           │  │
│ │  ┌─────────────────────────────────────────────────────┐ │  │
│ │  │ Metrics API Server (Port 8013)                      │ │  │
│ │  │ ─────────────────────────────────────────────────── │ │  │
│ │  │ • Serves fake metrics data                          │ │  │
│ │  │ • Returns CPU, memory, performance metrics          │ │  │
│ │  │ • Reads from: backend/data/metrics_data/            │ │  │
│ │  └─────────────────────────────────────────────────────┘ │  │
│ │                                                           │  │
│ │  ┌─────────────────────────────────────────────────────┐ │  │
│ │  │ Runbooks API Server (Port 8014)                     │ │  │
│ │  │ ─────────────────────────────────────────────────── │ │  │
│ │  │ • Serves fake runbook data                          │ │  │
│ │  │ • Returns troubleshooting guides, procedures        │ │  │
│ │  │ • Reads from: backend/data/runbooks_data/           │ │  │
│ │  └─────────────────────────────────────────────────────┘ │  │
│ │                                                           │  │
│ │  All 4 servers running on ONE computer!                  │  │
│ └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│ WHERE DOES THIS RUN?                                            │
│ ├── Currently: Your laptop (127.0.0.1) ❌ Gateway can't reach  │
│ └── Should be: EC2 instance with public domain ✅               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Key Point:** These are YOUR Python servers. They're just demo servers with fake data. In production, you'd replace these with real APIs to real systems.

---

### Layer 5: AgentCore Memory (AWS Service - The Brain)

```
┌─────────────────────────────────────────────────────────────────┐
│ AGENTCORE MEMORY (AWS Managed Service)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ This is like the agent's "long-term memory"                     │
│                                                                 │
│ What it stores:                                                 │
│ ├── User Preferences                                            │
│ │   └── "Alice prefers detailed technical reports"             │
│ │                                                               │
│ ├── Infrastructure Knowledge                                    │
│ │   └── "payment-service depends on database-service"          │
│ │                                                               │
│ └── Past Investigations                                         │
│     └── "Last week we fixed a similar CrashLoopBackOff issue"  │
│                                                                 │
│ WHERE DOES THIS RUN?                                            │
│ └── AWS Cloud (Amazon manages this for you)                    │
│     You just use it, you don't run it!                         │
│                                                                 │
│ YOUR MEMORY:                                                    │
│ ├── ID: sre_agent_memory-W7MyNnE0HE                            │
│ └── Status: ACTIVE                                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Key Point:** This is an AWS service. It remembers things across conversations so the agent gets smarter over time.

---

## The Complete Flow - Step by Step

Let me show you what happens when you ask "What pods are in CrashLoopBackOff?":

### Step 1: You Ask the Question

```
YOU → SRE Agent
"What pods are in CrashLoopBackOff?"
```

---

### Step 2: Supervisor Agent Plans

```
SUPERVISOR AGENT:
├── Checks Memory: "Does Alice have any preferences?"
│   └── Memory returns: "Alice likes detailed technical reports"
│
├── Checks Memory: "Have we seen similar issues before?"
│   └── Memory returns: "5 past investigations about pods"
│
└── Creates Plan:
    ├── Step 1: Use Kubernetes Agent to get pod status
    ├── Step 2: Use Logs Agent to check error logs
    └── Step 3: Use Runbooks Agent to get resolution steps
```

---

### Step 3: Kubernetes Agent Executes

```
KUBERNETES AGENT:
├── Needs to call: k8s-api___get_pod_status tool
│
└── Sends request to Gateway:
    "Hey Gateway, I need pod status for production namespace"
```

---

### Step 4: Gateway Processes Request

```
AGENTCORE GATEWAY:
├── Receives request from Kubernetes Agent
│
├── Checks JWT token: ✅ Valid
│
├── Looks up which backend API to call:
│   └── k8s-api tool → https://your-domain.com:8011/pods/status
│
├── Gets API key from Credential Provider:
│   └── Retrieves: 1a2db5e23451bdc3e9b42b265aa7278449a7e0171989eee91b4d9c8607aa0f7b
│
├── Calls backend API:
│   POST https://your-domain.com:8011/pods/status?namespace=production
│   Header: X-API-Key: 1a2db5e2...
│
└── ❌ FAILS because your-domain.com doesn't exist or points to 127.0.0.1
    Error: "Failed to obtain execution role credentials"
```

---

### Step 5: What SHOULD Happen (After Fix)

```
BACKEND API SERVER (on EC2):
├── Receives request from Gateway
│
├── Validates API key: ✅ Matches
│
├── Reads fake data from: backend/data/k8s_data/pods.json
│
└── Returns response:
    {
      "pods": [
        {
          "name": "database-pod-7b9c4d8f2a-x5m1q",
          "namespace": "production",
          "status": "CrashLoopBackOff",
          "phase": "Failed"
        },
        ...
      ]
    }
```

---

### Step 6: Agent Processes Response

```
KUBERNETES AGENT:
├── Receives pod data from Gateway
│
├── Analyzes: "Found 1 pod in CrashLoopBackOff"
│
└── Returns findings to Supervisor
```

---

### Step 7: Supervisor Generates Report

```
SUPERVISOR AGENT:
├── Collects results from all agents
│
├── Applies user preferences:
│   └── "Alice wants detailed reports" → Add technical details
│
├── Saves to Memory:
│   └── "Investigation about CrashLoopBackOff on 2026-02-20"
│
└── Generates final report:
    "Found 1 pod in CrashLoopBackOff: database-pod-7b9c4d8f2a-x5m1q
     Error logs show: Database connection timeout
     Recommended action: Check database connectivity"
```

---

## Deployment Options - Where Things Run

### Option 1: Local Testing (Current Setup)

```
┌─────────────────────────────────────────────────────────────────┐
│ YOUR LAPTOP                                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ SRE Agent (Python code)                                 │    │
│ │ Running via: uv run sre-agent                           │    │
│ └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ 4 Backend Servers                                       │    │
│ │ Running on: 127.0.0.1:8011-8014                         │    │
│ └─────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ AWS CLOUD                                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ AgentCore Gateway                                       │    │
│ │ ❌ Cannot reach 127.0.0.1 on your laptop!               │    │
│ └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ AgentCore Memory                                        │    │
│ │ ✅ Works fine                                            │    │
│ └─────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Problem:** Gateway can't reach your laptop!

---

### Option 2: Backend on EC2 (Recommended)

```
┌─────────────────────────────────────────────────────────────────┐
│ YOUR LAPTOP                                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ SRE Agent (Python code)                                 │    │
│ │ Running via: uv run sre-agent                           │    │
│ └─────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ AWS CLOUD                                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ AgentCore Gateway                                       │    │
│ │ ✅ Can reach EC2 instance!                               │    │
│ └─────────────────────────────────────────────────────────┘    │
│                     ↓                                           │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ EC2 INSTANCE                                            │    │
│ │ ├── Domain: my-backend.com                              │    │
│ │ ├── SSL: HTTPS enabled                                  │    │
│ │ └── 4 Backend Servers (Ports 8011-8014)                 │    │
│ └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ AgentCore Memory                                        │    │
│ │ ✅ Works fine                                            │    │
│ └─────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Solution:** Backend on EC2 with public domain!

---

### Option 3: Full Production (Agent + Backend on AWS)

```
┌─────────────────────────────────────────────────────────────────┐
│ AWS CLOUD                                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ AgentCore Runtime                                       │    │
│ │ ├── Your SRE Agent running as container                 │    │
│ │ └── Receives requests via API                           │    │
│ └─────────────────────────────────────────────────────────┘    │
│                     ↓                                           │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ AgentCore Gateway                                       │    │
│ │ ✅ Can reach EC2 instance!                               │    │
│ └─────────────────────────────────────────────────────────┘    │
│                     ↓                                           │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ EC2 INSTANCE                                            │    │
│ │ ├── Domain: my-backend.com                              │    │
│ │ ├── SSL: HTTPS enabled                                  │    │
│ │ └── 4 Backend Servers (Ports 8011-8014)                 │    │
│ └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────┐    │
│ │ AgentCore Memory                                        │    │
│ │ ✅ Works fine                                            │    │
│ └─────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Best:** Everything in AWS, fully managed!

---

## Key Verification from Official Diagram

✅ **What I Got Right:**
1. AgentCore Runtime runs your SRE agent (multi-agent system)
2. AgentCore Gateway sits between agent and backend APIs
3. AgentCore Memory stores session history and user preferences
4. AgentCore Identity (Cognito) handles authentication
5. Amazon Bedrock LLMs power the AI intelligence
6. Gateway cannot reach localhost (127.0.0.1) - needs public domain
7. Backend APIs need HTTPS with SSL certificates
8. The 4 tools connect to the Gateway via OpenAPI specs

✅ **Important Clarification:**
The diagram shows 4 backend APIs as **separate logical components** (different purposes), but does NOT mandate whether they run on:
- 1 EC2 instance (recommended for demo/small scale - cheaper, simpler)
- 4 separate EC2 instances (for larger scale - more expensive)
- Containers, Lambda, or other deployment options

**For this project's demo setup:** All 4 run on ONE EC2 instance because they're lightweight demo servers with fake data.

---

## Summary

**To answer your questions:**

1. **Are the 4 tools running on 4 different instances?**
   - The diagram doesn't specify - it shows logical architecture
   - This project's demo runs all 4 on ONE EC2 instance
   - You COULD run them on 4 separate instances if needed
   - Each listens on a different port (8011, 8012, 8013, 8014)

2. **Where does the code live?**
   - Backend code: On EC2's hard drive (or in a Docker container on EC2)
   - Agent code: Can run on your laptop, in Docker, or in AgentCore Runtime
   - Gateway & Memory: AWS managed services (you don't store code, just configure)

3. **What's the architecture?**
   - YOU → Agent → Gateway → Backend APIs → Data
   - Memory sits alongside to remember things
   - Gateway is the middleman that handles security and translation

**Current Problem:**
- Backend runs on your laptop (127.0.0.1)
- Gateway runs in AWS
- They can't talk to each other!

**Solution:**
- Move backend to EC2 with public domain
- Now Gateway can reach it!
